# node-api-postgres-jest
#### This project demonstrates a clean Node.js API architecture using PostgreSQL. It implements the Repository Pattern for database interactions, uses connection pooling for efficient performance, and supports database migrations.

### Features:

Node.js with PostgreSQL

Repository Pattern for organized database access

Connection pooling for better performance

Database migrations

Jest tests for unit and integration testing
